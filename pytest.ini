[pytest]
markers =
    integration: marks tests that require a live OpenD connection (deselect with -m "not integration")
# Keep mcp_servers subdirs off the automatic import path to prevent
# "server" module name collisions across the three MCP server test files.
norecursedirs = mcp_servers scripts .claude session data
